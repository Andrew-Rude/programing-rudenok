/**
 * @mainpage
 * # Завдання
 * Реалізувати програму, яка визначає кількість розрядів та суму цифр заданого числа.
 *
 * @details
 * Програма реалізована за допомогою трьох типів циклів: for, while, do-while. 
 * У репозиторії зберігається варіант із циклом `while-do`.
 *
 * @author
 * Rudenok A.
 *
 * @date
 * 15-dec-2024
 * @version 1.0
 */

/**
 * @file main.c
 * @brief Файл із реалізацією підрахунку кількості розрядів та суми цифр числа.
 *
 * @details
 * Програма приймає ціле число, обчислює кількість розрядів і суму його цифр.
 * Реалізація з використанням циклу while.
 */
 
 /**
 * Головна функція.
 *
 * Алгоритм роботи :
 * - Задається вхідне число.
 * - Підраховується кількість розрядів числа.
 * - Обчислюється сума цифр числа.
 * - Зупиняється виконання для перевірки результатів у відладчику.
 *
 * @return Код успішного завершення програми (0).
 */
 
int main() {
    // Вхідне число
    int number = 123456;

    // Ініціалізація змінних
    int digit_count = 0; // Кількість розрядів
    int digit_sum = 0;   // Сума цифр
    int temp = number;   // Копія числа для обчислень, щоб не змінювати оригінальне число

    // Цикл для підрахунку розрядів і суми цифр
    while (temp != 0) {         // Цикл while продовжує працювати, поки temp не стане рівним нулю
        digit_sum += temp % 10; // Додаємо останню цифру до суми
        temp /= 10;             // Відкидаємо останню цифру
        digit_count++;          // Збільшуємо лічильник розрядів
    }
    
    if (digit_sum == digit_count)
    {
    }
    // Зупинка для перевірки результатів
    return 0;
}

