/**
 * @mainpage
 * # Завдання
 * При заданих значеннях x та m, визначити значення y відповідно до формули:
 *
 * y = { x^2 - m^2, x > m
 *      5x^2 - x + 3m, x ≤ m }
 *
 * @author Rudenok A.
 * @date 12-dec-2024
 * @version 1.0
 */

/**
 * @file main.c
 * @brief Файл для обчислення значення y на основі значень x та m.
 *
 * Визначається значення y в залежності від порівняння x і m:
 * - Якщо x > m, то y = x^2 - m^2.
 * - Якщо x ≤ m, то y = 5x^2 - x + 3m.
 *
 * @author Rudenok A.
 * @date 12-dec-2024
 * @version 1.0
 */

#include <stdio.h>

/**
 * Головна функція.
 *
 * Послідовність дій:
 * - Задати значення x та m.
 * - Обчислити y в залежності від умови.
 * - Вивести результат у відладчик для перевірки.
 *
 * @return Код успішного завершення програми (0).
 */
 
int main() {
    // Задання значень
    double x = 3.0;  
    double m = 2.0;  
    double y;        // Результат обчислення

    // Обчислення y залежно від умови
    if (x > m) {
        y = (x * x) - (m * m);  // Якщо x > m
    } else {
        y = 5 * (x * x) - x + 3 * m;  // Якщо x ≤ m
    }

    // Зупинка виконання програми для перевірки результату в відладчику
    return 0;
}

